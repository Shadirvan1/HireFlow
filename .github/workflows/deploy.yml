name: HireFlow Auto-Deploy

on:
  push:
    branches: [ main ]

jobs:
  # --- JOB 1: RUN TESTS ---
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Backend Dependencies
        run: |
          cd backend
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Backend Tests
        env:
          SECRET_KEY: "temporary-key-for-testing"
          DEBUG: "True"
          DB_NAME: "test_db"
          DB_ENGINE: "django.db.backends.sqlite3"
          REDIS_HOST: "localhost"
        run: |
          cd backend
          python manage.py test 

  deploy:
    runs-on: ubuntu-latest
    needs: test  
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1

      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build & Push Backend
        run: |
          docker build -t 277436022464.dkr.ecr.ap-south-1.amazonaws.com/hireflow-backend:latest ./backend
          docker push 277436022464.dkr.ecr.ap-south-1.amazonaws.com/hireflow-backend:latest

      - name: Build & Push AI Service
        run: |
          docker build -t 277436022464.dkr.ecr.ap-south-1.amazonaws.com/hireflow-ai-service:latest ./ai_service
          docker push 277436022464.dkr.ecr.ap-south-1.amazonaws.com/hireflow-ai-service:latest

      - name: Update EKS Cluster
        run: |
          # Use your correct cluster name here
          aws eks update-kubeconfig --name hire-flow --region ap-south-1
          kubectl rollout restart deployment/backend-app
          kubectl rollout restart deployment/celery-beat
          kubectl rollout restart deployment/celery-worker
          kubectl rollout restart deployment/ai-service-deployment
          kubectl rollout restart deployment/ai-celery-worker
          kubectl apply -f k8s/config.yaml